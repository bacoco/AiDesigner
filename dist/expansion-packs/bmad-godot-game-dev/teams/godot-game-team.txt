- Legacy bundles may still reference `.bmad-core` paths. Treat them the same as the current root prefix described above.
- `.agilai-core/` folder created with all core agents
If a YAML Template has not been provided, list all templates from .agilai-core/templates or ask the user to provide another.
- Read `.agilai-core/core-config.yaml` and resolve `qa_root`, `story_root`, and `project_root`
- Missing `.agilai-core/core-config.yaml`
- If the file does not exist, HALT and inform the user: "core-config.yaml not found. This file is required for story creation. You can either: 1) Copy core-config.yaml from GITHUB agilai-core/ and configure it for your game project OR 2) Run the BMad installer against your project to upgrade and add the file automatically. Please add and configure before proceeding."
- Read `.agilai-core/core-config.yaml` and resolve `qa_root`, `story_root`, and `project_root`
- Missing `.agilai-core/core-config.yaml`
If a YAML Template has not been provided, list all templates from .agilai-core/templates or ask the user to provide another.
- If the file does not exist, HALT and inform the user: "core-config.yaml not found. This file is required for story creation. You can either: 1) Copy core-config.yaml from GITHUB agilai-core/ and configure it for your game project OR 2) Run the BMad installer against your project to upgrade and add the file automatically. Please add and configure before proceeding."
- `.agilai-core/` folder created with all core agents
